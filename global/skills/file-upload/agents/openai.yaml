interface:
  display_name: "File Upload"
  short_description: "Publish a local file through your upload service"
  default_prompt: "Use $file-upload to publish these files and return their shareable links."
